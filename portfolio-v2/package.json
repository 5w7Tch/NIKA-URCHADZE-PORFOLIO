{
  "name": "nika-urchadze-portfolio",
  "private": true,
  "version": "2.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "vite build && rm -rf ../assets && cp -R dist/. .."
  },
  "dependencies": {
    "@mediapipe/tasks-vision": "^0.10.35",
    "d3-delaunay": "^6.0.4",
    "three": "^0.178.0"
  },
  "devDependencies": {
    "vite": "^7.0.0"
  }
}
